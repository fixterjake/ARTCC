name: Build & Push Dev
on:
  push:
    branches:
        - develop
jobs:
  DockerBuildPush:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
    - name: Checkout the repo 
      uses: actions/checkout@v2
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Build images
      run: |
        cd api && docker build -t api -f ARTCC.Core.API/Dockerfile .
    - name: Tag images
      run: | 
        docker tag api ghcr.io/fixterjake/api:dev
    - name: Push images
      run: |
        docker push ghcr.io/fixterjake/api:dev